'use strict';

/**
 * family-diversity-exhibit controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::family-diversity-exhibit.family-diversity-exhibit');
