'use strict';

/**
 * family-diversity-exhibit router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::family-diversity-exhibit.family-diversity-exhibit');
