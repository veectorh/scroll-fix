'use strict';

/**
 * family-diversity-exhibit service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::family-diversity-exhibit.family-diversity-exhibit');
